<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.vmlt.cinema">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cinema"
        tools:targetApi="31">
        <activity
            android:name="com.vmlt.cinema.ui.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Cinema">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.details.MovieDetailsActivity"
            android:exported="true"
            android:parentActivityName="com.vmlt.cinema.ui.main.MainActivity"
            android:theme="@style/Theme.Cinema">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.vmlt.cinema.ui.main.MainActivity" />
        </activity>

        <activity
            android:name=".ui.tickets.TicketsActivity"
            android:exported="true"
            android:parentActivityName=".ui.details.MovieDetailsActivity"
            android:theme="@style/Theme.Cinema">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.details.MovieDetailsActivity" />
        </activity>
    </application>

</manifest>